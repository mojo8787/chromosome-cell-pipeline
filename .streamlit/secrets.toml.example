# Copy to secrets.toml and add your key for local development.
# For Streamlit Community Cloud, add these in App settings → Secrets.
# DO NOT commit secrets.toml (it is gitignored).

OPENAI_API_KEY = "sk-your-key-here"
